import { FunctionComponent } from "react";

export const Loading: FunctionComponent = () => (
  <button className="btn loading mt-5 w-full flex-auto flex-wrap bg-amber-500	">
    Transaction waiting to be mined
  </button>
);
